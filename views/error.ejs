<!DOCTYPE html>
<html class="has-background-grey" lang="en">

<head>  
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <title>
    Error: FASTER Web Helper
  </title>

  <link rel="icon" href="<%= urlPrefix %>/images/logo.svg" />
  <link rel="stylesheet" href="<%= urlPrefix %>/lib/bulma/bulma.min.css" />
  <link rel="stylesheet" href="<%= urlPrefix %>/lib/fa/css/all.min.css" />
</head>

<body>
  <div class="columns is-centered">
    <div class="column is-half-widescreen is-two-thirds-desktop is-three-quarters-tablet">
      <main class="message is-danger mx-3 my-6">
        
        <div class="message-body">
          <h1 class="title is-3 has-text-centered">
            <%= message %>
            <% if (error.status !== undefined) { %>
              <br />
              <small><%= error.status %></small>
            <% } %>
          </h1>
          <% if (error.stack !== undefined) { %>
            <pre tabindex="0"><%= error.stack %></pre>
          <% } %>

          <p class="has-text-centered mt-4">
            <a href="<%= urlPrefix %>/login" class="button is-link">
              <span class="icon">
                <i class="fas fa-sign-in-alt"></i>
              </span>
              <span>Return to Login</span>
            </a>
          </p>
        </div>
      </main>
    </div>
  </div>
</body>
</html>